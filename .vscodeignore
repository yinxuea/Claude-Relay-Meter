# 源代码文件（已编译到 out/）
src/**
tsconfig.json

# 开发和测试文件
.vscode/**
.vscode-test/**
.claude/**

# 构建和依赖
# 注意：不排除 node_modules，让 vsce 自动包含所有生产依赖
# 这样可以确保所有嵌套依赖都被正确打包

# Git 相关
.git/**
.gitignore

# 开发脚本和工具
scripts/**

# 文档（只保留核心文档）
# 保留：README.md, CLAUDE.md, RELEASE.md, 快速使用指南.md
# 以下文档已被删除或不需要：
QUICKSTART.md
PROJECT_SUMMARY.md
MODIFICATION_SUMMARY.txt
CHANGELOG.md
RELEASE_SUMMARY.md
expImg/**
images/**

# 包管理文件（只保留 package.json）
yarn.lock
package-lock.json

# 编译和打包
builds/**
*.vsix

# 日志和临时文件
*.log
npm-debug.log*
.DS_Store

# 配置示例
.vscode/settings.json.example

# ESLint 配置
.eslintrc*
eslint.config.mjs

# 其他开发文件
*.tmp
*.bak
